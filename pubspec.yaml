    name: agendanova
    description: "A new Flutter project."
    publish_to: 'none' # Remove this line if you wish to publish to pub.dev

    version: 1.0.0+1

    environment:
      sdk: ^3.8.1 # Ou a versão do SDK que você está usando (conforme flutter doctor)

    dependencies:
      flutter:
        sdk: flutter
      cupertino_icons: ^1.0.8
      go_router: ^13.0.1
      firebase_core: ^2.25.3
      cloud_firestore: ^4.14.4
      firebase_auth: ^4.16.0
      provider: ^6.1.1
      intl: ^0.18.0
      table_calendar: ^3.0.9
      get_it: ^7.6.0 # Adicione esta linha

    dev_dependencies:
      flutter_test:
        sdk: flutter
      flutter_lints: ^3.0.0 # Ajustado para uma versão mais recente de lints
    
    flutter:
      uses-material-design: true
    