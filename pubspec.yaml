    name: agenda_treinamento
    description: "A new Flutter project."
    publish_to: 'none' # Remove this line if you wish to publish to pub.dev

    version: 1.0.0+1

    environment:
      sdk: ^3.11.4 #^3.9.3 Ou a versão do SDK que você está usando (conforme flutter doctor)

    dependencies:
      flutter:
        sdk: flutter
      cupertino_icons: ^1.0.9
      go_router: ^17.0.1
      firebase_core: ^4.1.1
      cloud_firestore: ^6.2.0
      firebase_auth: ^6.3.0
      provider: ^6.1.5
      intl: ^0.20.2
      table_calendar: ^3.2.0
      get_it: ^9.2.1
      logger: ^2.7.0
      collection: ^1.19.0

    dev_dependencies:
      flutter_test:
        sdk: flutter
      integration_test:
        sdk: flutter
      http: ^1.2.1
      flutter_lints: ^6.0.0
      flutter_launcher_icons: ^0.14.4
      mockito: ^5.6.4
      build_runner: ^2.13.1
      test: ^1.30.0

    flutter:
      uses-material-design: true

    flutter_launcher_icons:
      android: true
      image_path: "assets/icon/icon.png" # Caminho para a sua imagem principal