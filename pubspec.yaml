    name: agendanova
    description: "A new Flutter project."
    publish_to: 'none' # Remove this line if you wish to publish to pub.dev

    version: 1.0.0+1

    environment:
      sdk: ^3.8.1 # Ou a versão do SDK que você está usando (conforme flutter doctor)

    dependencies:
      flutter:
        sdk: flutter
      cupertino_icons: ^1.0.8
      go_router: ^16.0.0
      firebase_core: ^3.15.0
      cloud_firestore: ^5.6.10
      firebase_auth: ^5.6.1
      provider: ^6.1.5
      intl: ^0.20.2
      table_calendar: ^3.2.0
      get_it: ^8.0.3

    dev_dependencies:
      flutter_test:
        sdk: flutter
      flutter_lints: ^6.0.0
    
    flutter:
      uses-material-design: true
    