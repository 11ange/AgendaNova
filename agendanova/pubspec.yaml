name: agendanova
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^13.0.1 # Verifique a versão mais recente
  firebase_core: ^2.25.3 # Verifique a versão mais recente
  cloud_firestore: ^4.14.4 # Verifique a versão mais recente
  firebase_auth: ^4.16.0 # Verifique a versão mais recente
  provider: ^6.1.1 # Adicione esta linha
  intl: ^0.18.0 # Adicione esta linha

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
